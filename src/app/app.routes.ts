import { Routes } from '@angular/router';
import { TaskWrapperComponent } from './components/task-wrapper/task-wrapper.component';

export const routes: Routes = [
  { path: 'tasks', component: TaskWrapperComponent },
];
